@{
    ViewData["Title"] = "Nạp tiền";
    Layout = "~/Views/Shared/_EmployerLayout.cshtml";
}

<div class="main-content">
    <div class="page-header mb-4">
        <h2 class="fw-bold text-primary"><i class="fas fa-wallet me-2"></i>Nạp tiền vào tài khoản</h2>
        <p class="text-muted fst-italic">
            Lưu ý: <strong>1,000 VNĐ = 1 Credit (CD)</strong>. Số credit này được dùng để mua lượt đăng tin, đẩy bài, hoặc các dịch vụ khác.
        </p>
    </div>

    <div class="card shadow-sm border-0">
        <div class="card-body">
            <form asp-action="CreatePayment" method="post">
                <div class="mb-3">
                    <label for="amount" class="form-label fw-semibold">Số tiền muốn nạp (VNĐ)</label>
                    <input id="amount" name="amount" type="number" class="form-control" required min="2000" step="1000" placeholder="Nhập số tiền tối thiểu 2,000 VNĐ" />
                </div>

                <button type="submit" class="btn btn-primary px-4">
                    <i class="fas fa-paper-plane me-1"></i> Nạp tiền
                </button>
            </form>
        </div>
    </div>
</div>
